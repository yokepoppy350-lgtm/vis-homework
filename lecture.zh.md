# 如何做好页面布局

一个学习数据看板，从混乱到清晰

## 6.1 信息分区与视觉层级

先决定用户需要看到什么，再决定元素放在哪里。本章始终改进同一个学习数据看板：帮助学生快速了解学习进度，再查看趋势和课程分布。

### 操作任务

把重要指标放在趋势图之前，并用区域边界检查信息分组。比较改进前后，你能否更快找到本周学习时长？

### 从用户任务确定顺序

本案例的首要任务是快速了解学习状况，次要任务是分析原因。因此先呈现指标摘要，再呈现趋势与课程分布。先画出内容清单并标记主次，可避免把所有模块设计得同样突出。

### 用区域表达内容关系

导航用于选择位置，工具栏用于执行操作，主区域用于阅读结果。将同一任务的内容靠近，并保留组间留白。背景和边界可以辅助分组，但不能替代合理的内容组织。

### 建立可辨认的层级

本周时长等关键数字需要突出；模块标题次之；时间范围和单位作为辅助信息。通过字号、字重、位置与留白建立差异。不要仅靠颜色传达主次，也不要把所有标题放大。

### 文档顺序也是阅读顺序

语义结构应在没有装饰时仍能阅读。CSS 改变位置不一定改变键盘或屏幕阅读器的顺序。本案例让摘要在图表之前出现在文档中；示例的“改进前”只是教学反例，不应作为最终布局。

### 关键点

先用信息分区与顺序解决“看什么”，再用视觉层级解决“先看什么”。

### 为什么这么写

侧栏固定宽度，内容列用 minmax(0,1fr) 接收剩余空间；摘要先于图表出现在文档中，让视觉顺序和阅读任务保持一致。

### 常见错误

不要只用 CSS order 把重要内容挪到前面：键盘与辅助阅读顺序可能仍跟随文档。改进前视图用于观察问题，正式页面应直接采用合理的内容顺序。

[独立演示](demo/index.html?lesson=1&lang=zh)

## 6.2 留白与间距系统

沿用上一节的信息结构，调整学习指标卡的内部空间。留白要表达内容关系，同时保证卡片在有限宽度内可读。

### 操作任务

固定声明宽度，增大内边距，再切换两种盒模型。观察内容宽度和边框盒宽度哪个发生变化，并解释原因。

### 组内紧，组间松

“本周学习”和“18.5 小时”属于一组，应该比它们与下一张卡片的距离更近。这体现亲近性：距离帮助人判断哪些内容属于一起。留白不是剩下来的空间，而是组织信息的工具。

### 建立尺度，而不是死记数字

可从 4、8、16、24、32px 选择一组常用间距，降低随意性。它不是禁止其他数字的规则。应根据内容密度、文字长度和屏幕空间调整，并在同类组件中保持一致。

### 区分声明宽度和占用宽度

content-box 的 width 指内容宽度，边框盒宽度还要加左右 padding 和 border。border-box 的 width 已包含这些部分。margin 位于边框外，不包含在两种 width 中。本演示显示实际盒尺寸和包含外边距的水平占用宽度。

### 让间距服务阅读

过小的内边距使文字贴近边缘，过大则压缩内容空间，可能造成频繁换行。不能用一个固定数值断言所有页面都“合理”。请结合当前卡片的标题、数值和说明判断，并比较英文模式。

### 关键点

先判断空白表达的是组件内部空间还是组件之间的关系，再选 padding、gap 或 margin。

### 为什么这么写

border-box 将内边距与边框纳入声明宽度，适合控制卡片边界；content-box 则便于观察内容宽度之外的累加。两种模式下都单独计算外边距。

### 常见错误

不要把包含外边距的水平占用称为元素 width；也不要把内边距无限增大。相同声明宽度下，border-box 的内容区域会变窄，需要检查文本换行。

[独立演示](demo/index.html?lesson=2&lang=zh)

## 6.3 局部排列与对齐

为同一个看板安排顶部筛选栏：搜索框能够伸缩，课程筛选保持可读，空间不足时允许换行。

### 操作任务

切换较长的文字，将预览宽度调到 390px，比较开启与关闭换行的结果。随后检查主轴与交叉轴变化。

### 对齐提供稳定的参照

工具栏里的搜索和筛选属于同一个任务组。统一边界与垂直对齐可以减少视觉跳跃。字号或控件高度不同时，需要有意识地选择居中、起点或基线对齐，而不是逐个移动元素。

### 先判断一维关系

工具栏主要处理一行内容的排列，适合 Flex。direction 决定主轴，justify-content 沿主轴分配空间，align-items 沿交叉轴对齐。改为 column 后，主轴变成纵向，这些属性的方向也随之改变。

### 定义谁可以伸缩

搜索区域使用 flex:1 1 220px，允许参与剩余空间分配；课程选择区域保留自身内容尺寸。比起给所有控件固定宽度，这种规则更能表达任务优先级，但仍要检查最小可读宽度。

### 换行是一种布局决策

空间不足时换行通常比挤压文字更可读。gap 保持成员之间的统一距离，不增加容器外边缘的额外间距。若需要每一行的列严格对应，则应使用 Grid，而不是反复调整 Flex 子项。

### 关键点

用 Flex 描述局部成员的关系：谁伸缩、如何对齐、何时换行。

### 为什么这么写

搜索区允许增长和收缩，筛选区保留内容尺寸；wrap 与 gap 共同维持小屏下的组内关系。列方向会同时改变主轴，因此对齐要结合 direction 理解。

### 常见错误

若给每个成员固定宽度并关闭换行，长文本容易溢出。实验中可故意关闭 wrap 观察问题；最终方案应检查中英文长文本，而不是只看短标签。

[独立演示](demo/index.html?lesson=3&lang=zh)

## 6.4 整体网格与主次关系

把指标、学习趋势和课程分布放进同一个网格。通过比例和跨列规则，让主要信息获得足够的阅读空间。

### 操作任务

比较等分、主次两列和跨列布局。观察主图的可读宽度，打开列线，核对下方代码与实际轨道。

### 网格用于协调多个区域

网格建立共享的列边界，让摘要、图表和说明具有秩序。它不仅是把内容平均切成几份。先确定模块的作用与最低空间需求，再决定列数和跨度。

### 比例应反映信息价值

学习趋势需要展示时间变化，通常比课程分布需要更宽的区域。本案例用 2:1 比例作为起点；若任务变成比较课程，等分也可能合理。布局优劣取决于任务，不存在通用最佳比例。

### 理解剩余空间与最小尺寸

fr 分配轨道中的可用空间。minmax(0,1fr) 允许轨道收缩，子项仍需 min-width:0 等规则避免内容撑破。允许缩小不代表文字会自动变得易读，所以还需要在窄屏重排。

### 跨列建立共同边界

摘要横跨两列，可以与下方两张图表的整体外边界对齐。本演示的列线来自同一列模板，并包含当前 gap；它表示实际轨道，而非装饰性的背景方格。

### 关键点

让内容决定网格，让网格维持秩序；用同一组轨道协调模块边界。

### 为什么这么写

共享列模板使不同模块具有共同边界；主次 2:1 为趋势留出更多空间，跨列让摘要与整体网格对齐。辅助线使用相同轨道与 gap。

### 常见错误

不要给两张卡片各设 50% 后再加 gap，也不要把 fr 理解成固定像素。允许轨道收缩仍需检查内容是否可读，窄屏可能需要改单列。

[独立演示](demo/index.html?lesson=4&lang=zh)

## 6.5 响应式与内容保留

缩窄同一个看板，观察它从单列到多列的变化。演示使用独立页面视口和真实媒体查询，宽度数值就是内部视口宽度。

### 操作任务

依次查看 519、520、759、760px，说明哪个规则开始生效。确认课程分布没有消失，并尝试窄屏中的导航菜单。

### 断点由内容决定

本案例以 520px 和 760px 作为经过布局安排的演示断点。它们不是所有设备的固定分类。实际项目应逐渐缩窄页面，在模块开始拥挤或失去可读性时决定重排位置。

### 先保障窄屏任务

默认样式为单列，520px 起摘要变为多列，760px 起出现侧栏和两列图表。这就是从简单布局逐步增强。更宽的屏幕用于增加同时呈现的信息，而不是让字号无限变大。

### 重排，不丢失内容

窄屏把课程分布移动到主图下方，导航改为可展开菜单。重要内容不能因为屏幕变窄就无法访问。重排时保持合理文档顺序，并检查控件是否仍可操作。

### 区分预览和外层网站

拖动滑块改变的是预览内部视口，不是浏览器整个窗口。外层屏幕放不下预览时可以横向滚动查看，系统不会偷偷缩小画布或改变标注数值。外层教学页面本身也需要独立的小屏适配。

### 关键点

响应式的目标是保留任务和信息，让内容在不同空间中重新组织。

### 为什么这么写

单列是默认状态，520px 和 760px 逐步增加列数与导航区域。演示使用真实独立视口，同一套媒体查询用于代码展示和实际渲染。

### 常见错误

不要把 520/760px 当成所有设备的标准，也不要仅靠隐藏图表解决空间不足。检查 519/520/759/760px 边界附近，以及导航展开后的内容可达性。

[独立演示](demo/index.html?lesson=5&lang=zh)

## 6.6 综合改进与自检

将前五节应用到同一个学习数据看板，比较初始反例与改进版。最终成果不仅要好看，还应能解释每一项布局选择。

### 操作任务

在桌面和手机宽度分别比较前后版本。用层级、间距、对齐、内容保留四项标准检查改进版，再切换语言验证长文本。

### 用任务验证层级

能否先找到本周时长、完成课程和目标进度？能否进一步定位趋势与课程分布？用具体查找任务验证阅读路径，比只问页面是否漂亮更有依据。

### 组合工具，不堆叠补丁

Grid 安排整体区域，Flex 排列局部工具栏，卡片内部使用正常文档流。用统一间距与最小宽度约束连接它们。局部组件应可以改变内容，而不依赖其他模块的偶然尺寸。

### 检查语言和尺寸变化

英文可能占据更长的横向空间。切换语言后，应保留当前章节和演示参数，再观察按钮、标题、卡片是否换行合理。窄屏不能通过隐藏主要内容来掩盖溢出问题。

### 区分布局原型与数据产品

图表与数值是用于教学的模拟内容，重点是模块布局，不能据此得出真实学习表现结论。筛选栏在这里演示排列关系；完整数据查询和导出功能不属于本章目标，因此不放置无效的导出按钮。

### 关键点

用可解释的原则、可运行的代码和可观察的交互共同证明布局改进。

### 为什么这么写

Grid 负责整体，Flex 负责工具栏，卡片内采用正常文档流。共享规则让同一案例在不同尺寸和语言下保持一致的内容关系。

### 常见错误

不要把装饰方格当作真实列线，也不要用删掉次要图表制造“适配成功”。先比较查找任务、内容保留与可读性，再调整颜色、阴影等细节。

[独立演示](demo/index.html?lesson=6&lang=zh)

