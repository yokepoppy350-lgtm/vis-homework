# 第6章：交互式数据可视化

课程作业网页，使用 HTML + CSS + JavaScript + D3.js v7。

## 内容
- 6.1 Tooltip 信息提示
- 6.2 筛选与动态更新
- 6.3 Brush 区域选择
- 6.4 Zoom 缩放与平移
- 6.5 多视图联动 Dashboard

## 发布
将本目录中的 `index.html`、`css/`、`js/`、`data/` 直接放在 GitHub Pages 仓库根目录。

v6 布局：单小节阅读器 + 上一节/下一节 + 多层级配色 + 字体层级调整。


## v7 交互修正
- Zoom Demo 改为图表内直接滚轮缩放、拖动平移、双击/按钮重置。
- 缩放范围为 1×–8×，达到边界后继续滚轮不会触发浏览器页面缩放。
- Demo 顶部显示实时缩放倍率与边界状态。
